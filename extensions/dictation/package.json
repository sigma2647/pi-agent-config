{
	"name": "pi-dictation",
	"private": true,
	"type": "module",
	"description": "Voice input for pi: offline SenseVoice model or cloud speech APIs",
	"pi": {
		"extensions": ["./index.ts"],
		"cli": [{ "name": "pi-dictation", "entry": "./dev.ts" }]
	},
	"scripts": {
		"test": "node --experimental-strip-types --no-warnings --test --test-timeout=60000 test/*.test.ts",
		"check": "tsc --noEmit -p tsconfig.json"
	},
	"dependencies": {
		"sherpa-onnx": "^1.13.8"
	}
}
